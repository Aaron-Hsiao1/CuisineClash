using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HPGameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        HPEventManager.OnTimerStart();
    }

    // Update is called once per frame
    void Update()
    {

    }
}